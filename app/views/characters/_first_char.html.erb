
<%= form_for(@character, html: { class: 'form-horizontal'}) do |f| %>
  <% if @character.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@character.errors.count, "error") %> prohibited this character from being saved:</h2>

      <ul>
      <% @character.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, :class => "control-label col-sm-3"  %> <i></i>
    <div class="col-sm-6"><%= f.select :name, Character.where(:user_id => nil).map{ |a| a.name },{}, { :class => 'form-control' } %>
    </div>
  </div>
  <%= f.hidden_field(:old_character, :value => 1) %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
